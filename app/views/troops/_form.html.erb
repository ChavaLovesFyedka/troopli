<%= form_for @troop, html: { multipart: true } do |f| %>
  <% if @troop.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@troop.errors.count, "error") %> prohibited this troop from being saved:</h2>

      <ul>
      <% @troop.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :picture %><br>
    <%= f.file_field :picture %>
  </div>
  <div class="field">
    <%= f.label :age_level %><br>
    <%= f.number_field :age_level %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
